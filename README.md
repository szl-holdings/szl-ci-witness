# szl-ci-witness
CI as witness: every workflow run appends a hash-chained receipt (repo, commit, run id, conclusion, test counts) to a verifiable chain. Regressions and fixes are detected from the chain, not from dashboards. Verify anyone's chain offline. Fail-closed.
